<UserControl x:Class="Phoenix.UI.WPF.Views.ReaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:local="clr-namespace:Phoenix.UI.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Reader Toolbar -->
        <Border Grid.Row="0" 
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="12,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- TOC Toggle and Book Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="☰" 
                            ToolTip="Toggle Table of Contents"
                            Command="{Binding DataContext.ToggleSidebarCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            Style="{DynamicResource IconButtonStyle}"
                            Foreground="{DynamicResource TextPrimaryBrush}"
                            FontSize="16" Padding="8,4" Margin="0,0,8,0"/>
                    
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding CurrentBook.Title}" 
                                   FontWeight="SemiBold" FontSize="13"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   TextTrimming="CharacterEllipsis" MaxWidth="300"/>
                        <TextBlock Text="{Binding CurrentChapter.Title}" 
                                   FontSize="11"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   TextTrimming="CharacterEllipsis" MaxWidth="300"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- Navigation and Actions -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <!-- Bookmarks Dropdown -->
                    <ToggleButton x:Name="BookmarksToggle"
                                  Content="🔖" 
                                  ToolTip="Bookmarks"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  Foreground="{DynamicResource TextPrimaryBrush}" 
                                  FontSize="14" Padding="8,4"
                                  Cursor="Hand"/>
                    
                    <Popup IsOpen="{Binding IsChecked, ElementName=BookmarksToggle, Mode=TwoWay}"
                           PlacementTarget="{Binding ElementName=BookmarksToggle}"
                           Placement="Bottom"
                           StaysOpen="False"
                           AllowsTransparency="True">
                        <Border Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="8"
                                Margin="0,4,0,0"
                                MinWidth="280"
                                MaxHeight="400">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Header -->
                                <Grid Grid.Row="0" Margin="4,0,4,8">
                                    <TextBlock Text="Bookmarks" 
                                               FontWeight="SemiBold" FontSize="14"
                                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <Button Content="+ Add" 
                                            HorizontalAlignment="Right"
                                            Command="{Binding AddBookmarkCommand}"
                                            Style="{DynamicResource PrimaryButtonStyle}"
                                            Padding="8,4" FontSize="11"/>
                                </Grid>
                                
                                <Separator Grid.Row="1" Margin="0,0,0,8"/>
                                
                                <!-- Bookmarks List -->
                                <ScrollViewer Grid.Row="2" MaxHeight="300" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding Bookmarks}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Padding="8,6" Margin="0,0,0,4"
                                                        Background="Transparent"
                                                        CornerRadius="4"
                                                        Cursor="Hand">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Border.InputBindings>
                                                        <MouseBinding MouseAction="LeftClick" 
                                                                      Command="{Binding DataContext.GoToBookmarkCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                      CommandParameter="{Binding}"/>
                                                    </Border.InputBindings>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        
                                                        <StackPanel Grid.Column="0">
                                                            <TextBlock Text="{Binding Title}" 
                                                                       TextTrimming="CharacterEllipsis"
                                                                       FontSize="13"
                                                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="{Binding ScrollPosition, StringFormat='Position: {0:F0}%'}" 
                                                                           FontSize="11"
                                                                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                                <TextBlock Text=" • " 
                                                                           FontSize="11"
                                                                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                                <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:MMM d, h:mm tt}'}" 
                                                                           FontSize="11"
                                                                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                            </StackPanel>
                                                        </StackPanel>
                                                        
                                                        <Button Grid.Column="1" 
                                                                Content="✕" 
                                                                ToolTip="Delete"
                                                                Command="{Binding DataContext.DeleteBookmarkCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Style="{DynamicResource IconButtonStyle}"
                                                                Foreground="{DynamicResource TextSecondaryBrush}"
                                                                FontSize="10" Padding="4,2"
                                                                VerticalAlignment="Center"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                                
                                <!-- Empty State -->
                                <StackPanel Grid.Row="2" 
                                            VerticalAlignment="Center" 
                                            HorizontalAlignment="Center"
                                            Margin="0,20"
                                            Visibility="{Binding Bookmarks.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Invert}">
                                    <TextBlock Text="No bookmarks yet" 
                                               FontSize="12"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Popup>
                    
                    <Button Content="◀" 
                            ToolTip="Previous Chapter"
                            Command="{Binding PreviousChapterCommand}"
                            IsEnabled="{Binding CanGoPrevious}"
                            Style="{DynamicResource IconButtonStyle}"
                            Foreground="{DynamicResource TextPrimaryBrush}"
                            FontSize="12" Padding="8,4"/>
                    
                    <TextBlock Text="{Binding CurrentChapterIndex, StringFormat='{}{0}'}"
                               VerticalAlignment="Center" Margin="8,0"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="12"/>
                    
                    <Button Content="▶" 
                            ToolTip="Next Chapter"
                            Command="{Binding NextChapterCommand}"
                            IsEnabled="{Binding CanGoNext}"
                            Style="{DynamicResource IconButtonStyle}"
                            Foreground="{DynamicResource TextPrimaryBrush}"
                            FontSize="12" Padding="8,4"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Content Area with WebView2 -->
        <Grid Grid.Row="1">
            <wv2:WebView2 x:Name="ContentWebView"
                          Source="about:blank"/>
            
            <!-- Loading Overlay -->
            <Border Background="#80FFFFFF"
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="150" Height="4"/>
                    <TextBlock Text="Loading chapter..." Margin="0,8,0,0"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Bottom Progress Bar - Compact -->
        <Border Grid.Row="2" 
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="12,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <ProgressBar Grid.Column="0" 
                             Value="{Binding ReadingProgress}"
                             Maximum="100" Height="4"
                             Background="{DynamicResource BorderBrush}"
                             Foreground="{DynamicResource PrimaryBrush}"/>
                
                <TextBlock Grid.Column="1" 
                           Text="{Binding ReadingProgress, StringFormat='{}{0:F0}%'}"
                           Margin="10,0,0,0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           FontSize="11"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
